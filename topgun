#!/usr/bin/env bash
set -euo pipefail

COMMAND="${1:-help}"
shift || true

case "$COMMAND" in
  init)
    bash /topgun/commands/init.sh "$@"
    ;;
  help|--help|-h)
    echo "topgun — coding agent scaffolding tool"
    echo ""
    echo "Usage: topgun <command>"
    echo ""
    echo "Commands:"
    echo "  init    Copy topgun template into the current project"
    echo "  help    Show this help"
    ;;
  *)
    echo "Unknown command: $COMMAND" >&2
    echo "Run 'topgun help' for usage." >&2
    exit 1
    ;;
esac
